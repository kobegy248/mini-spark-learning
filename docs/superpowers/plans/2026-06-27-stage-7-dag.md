# 第七阶段 DAG 实现计划

目标：将 RDD lineage 转换成 ExecutionDAG，并根据宽窄依赖划分 Stage。

实施内容：

- 新建 `mini_spark/dag.py`
  - `DAGNode`
  - `Stage`
  - `ExecutionDAG`
- `ExecutionDAG.from_rdd(rdd)` 从 RDD parent 链生成 DAG。
- `ExecutionDAG.stages()` 根据 `wide` 依赖切 Stage。
- 新增测试 `tests/test_stage_7_dag.py`。
- 新增中文教程 `docs/07-dag.md`。
- 新增 HTML 可视化 `docs/visualizations/07-dag.html`。

验证：

```powershell
& '.\.venv\Scripts\python.exe' -m pytest tests -q
```

预期：

```text
38 passed
```
